{
  "name": "@sla/core",
  "version": "1.0.0",
  "private": true,
  "description": "Shared CSV parsing, validation, normalization, dedup and SLA math — imported by the Worker and the test suite so no logic is duplicated or scattered into the frontend.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
